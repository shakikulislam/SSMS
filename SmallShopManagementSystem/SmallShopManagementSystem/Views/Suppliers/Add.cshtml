@model SSMS.Models.Models.Supplier

@{
    ViewBag.Title = "Add";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Styles
{

}

<h1>Add Supplier</h1>
@*@Html.ActionLink("View","Show")*@
@using (Html.BeginForm("Add", "Suppliers", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    
    if (ViewBag.SMsg != null)
    {
        <p>@ViewBag.SMsg</p>
    }
    if (ViewBag.FMsg != null)
    {
        <p>@ViewBag.FMsg</p>
    }
    <table>
        <tr><td>@Html.LabelFor(c=>c.Code)</td></tr>
        <tr><td>@Html.TextBoxFor(c=>c.Code)</td></tr>
        <tr><td>@Html.LabelFor(c=>c.Name)</td></tr>
        <tr><td>@Html.TextBoxFor(c=>c.Name)</td></tr>
        <tr><td>@Html.LabelFor(c=>c.Address)</td></tr>
        <tr><td>@Html.TextBoxFor(c=>c.Address)</td></tr>
        <tr><td>@Html.LabelFor(c=>c.Email)</td></tr>
        <tr><td>@Html.TextBoxFor(c=>c.Email)</td></tr>
        <tr><td>@Html.LabelFor(c=>c.Contact)</td></tr>
        <tr><td>@Html.TextBoxFor(c=>c.Contact)</td></tr>
        <tr><td>@Html.LabelFor(c=>c.ContactPerson)</td></tr>
        <tr><td>@Html.TextBoxFor(c => c.ContactPerson)</td></tr>


        <tr><td></td></tr>
        @*<tr><td><img src="#" class="imageClass"/></td></tr>*@

        <tr><td><input type="file" name="UploadImage" class="btn btn-primary"/></td></tr>

        <tr><td><input type="submit" value="Add"/></td></tr>

    </table>
}
<div>

    file = Convert.ToBase64String(purchaseFile.File);
    var image = string.Format("data:image/gif; base64,{0}", UploadImage);
    <img src="data:image/png; base64, @Convert.ToBase64String(Model.Image,0,Model.Image.Length)" width="100px" height="100px" />
</div>
